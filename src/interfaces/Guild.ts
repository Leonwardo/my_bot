export interface GuildData {
    global: {
        join: {
            text: string
            color: string
        },
        leave: {
            text: string
            color: string
        }
    }
}